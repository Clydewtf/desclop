{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Allows the main window to use core Tauri IPC and open the folder dialog.",
  "windows": ["main", "quick-capture"],
  "permissions": ["core:default", "core:window:allow-hide", "dialog:allow-open"]
}
