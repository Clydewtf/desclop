{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Allows the main window to use core Tauri IPC and open the folder dialog.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-resizable",
    "dialog:allow-open"
  ]
}
